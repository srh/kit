Some feature differences between s1 and s3:
  Superset of s1:
  - s3 has gotos.
  - s3 has o8, o16, o32, diff.
  - s3 has incr/decr and "+="-type operators.
  - s3 allows negation for overflow types.
  - s3 allows deftype names to shadow def names.

  Subset of s1:
  - s3 disallows assignment except for top-level exprs.
  - s3 disallows strinits nakedly in conditionals/swartch locations.
  - s3 enum constructor type-checking/lookup is different.
  - s3 disallows whole field lookup.

